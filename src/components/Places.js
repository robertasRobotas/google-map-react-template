import React from 'react';

const Places = ({ width, height }) => {
  return (
    <div
      style={{
        height: height,
        width: width,
        background: 'white',
        float: 'left',
      }}></div>
  );
};

export default Places;
