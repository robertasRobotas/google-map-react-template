import React from 'react';

const Map = ({ height, width }) => {
  return (
    <div
      style={{
        height: height,
        width: width,
        background: 'purple',
        float: 'right',
      }}></div>
  );
};

export default Map;
